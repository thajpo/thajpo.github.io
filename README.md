## Local development

Run the site locally with Docker:

```sh
./bin/dev
```

Then open <http://localhost:4000>.

The command installs the Ruby gems inside the container and runs Jekyll with
live reload, so edits to pages, includes, layouts, Sass, and project markdown
are rebuilt automatically.
